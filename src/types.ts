/**
 * Squad-SpecKit Bridge
 *
 * Shared entity types and validation — innermost Clean Architecture layer.
 * Zero imports, zero I/O, pure data types and business rules.
 */
export interface BridgeConfig {
  contextMaxBytes: number;
}
